# PawBridge 파이썬 에이전트 지침

## 적용 범위와 판단 근거

- 이 지침은 `pawbridge-python` 전체에 적용한다. 하위 경로를 수정하기 전에 가장 가까운
  `AGENTS.md`를 확인한다.
- 현재 동작은 소스, 의존성 파일, 테스트와 실행 설정에서 확인한다.
  과거 README의 모델 이름이나 벡터 차원을 현재 계약으로 단정하지 않는다.
- 계획, 결정과 조사 기록은 Obsidian `Projects/pawbridge`에서 관리한다.
  코드와 함께 버전 관리해야 하는 API·실행 절차·복구 문서는 저장소에 유지한다.
- 로컬 수정, 테스트, 이미지 빌드, 이미지 게시, 배포와 운영 검증을 구분해서 보고한다.

## 작업 단위 일괄 승인

- 이 절은 사용자가 승인한 PawBridge 한정 예외다. 이 저장소와 연결 작업 폴더에서는
  전역 지침의 Git 작업별 재승인 대신 아래 절차를 따른다. 다른 프로젝트에는 적용하지 않는다.
- 시작할 때 목적, 변경 범위, 대상 저장소, 기준 브랜치와 SHA, 작업 브랜치와 폴더,
  검증 방법, 커밋·푸시·PR 생성 포함 여부를 묶어서 제시하고 한 번 승인받는다.
- 승인에 포함된 브랜치·작업 폴더 생성 또는 전환, 구현, 검증, 일반 커밋,
  해당 기능 브랜치 푸시, PR 생성과 해당 PR 제목·본문 수정은 매번 다시 묻지 않는다.
  커밋 제목과 PR 설명은 실제 변경·검증 결과에 맞게 작성하고 실행 결과를 보고한다.
- 구현 요청만으로 커밋·푸시·PR 생성 권한을 추정하지 않는다. 이미 제시한 일괄 범위에
  대한 사용자의 승인은 같은 작업의 후속 턴에도 유지하며, 턴 전환만으로 재승인받지 않는다.
- 범위·대상 저장소·기준 브랜치가 달라지면 변경 부분만 다시 승인받는다.
  리뷰 요청·멘션·댓글 등 다른 사람에게 보내는 메시지는 일괄 승인에 포함하지 않는다.
- 병합과 운영 배포는 검증 결과, 대상, 롤백 방법을 제시한 뒤 최종 승인받는다.
  사용자가 두 단계를 명시적으로 묶어 승인했다면 단계마다 다시 묻지 않는다.
- 파괴적 작업, 데이터 삭제·일회성 데이터 변경, 운영 비밀값 변경은 별도 승인 대상이다.
  강제 푸시와 공유 이력 재작성은 금지한다. 일괄 승인은 main/dev 직접 푸시나
  도구의 샌드박스·권한 제한 우회를 허용하지 않는다.
- 기존 작업 폴더에서 이어갈 때도 이 저장소 기준본의 최신 승인 규칙을 확인한다.
  과거 AGENTS.md 사본의 단계별 재승인 문구를 현재 합의로 취급하지 않는다.

## Git과 PR

- 기본 기준은 최신 `origin/dev`다. 브랜치·작업 폴더 생성 전에 원격 갱신 여부,
  정확한 기준 SHA, `HEAD...base` 분기 상태와 실질 변경 내역을 확인한다.
  갱신 실패 시 원격 상태를 최신이라고 보고하지 않는다.
- 기준 브랜치, 작업 브랜치, 작업 폴더와 검증 범위를 작업 단위 일괄 승인에 포함한다.
- 한 브랜치는 주된 검토 목적이 하나인 PR 하나로 유지한다. 문서·리팩터링·모델 교체·운영 배포를
  서로 무관한 변경과 섞지 않는다. 하나의 계약상 분리할 수 없다면 그 이유를 설명한다.
- 기존 변경이 있으면 별도 작업 폴더를 사용한다. 사용자 변경을 초기화하거나 폐기하지 않는다.
- `git add -A`나 `git add .` 대신 담당한 정확한 경로만 추가한다.
- 브랜치 접두사는 `feat/`, `fix/`, `ci/`, `docs/`, `chore/`를 사용하고,
  커밋 제목은 `type: 한글 명사구`로 작성한다.
- PR은 `.github/PULL_REQUEST_TEMPLATE.md`에 맞춰 작성하고 실제 수행한 검증만 체크한다.

## API·모델·데이터 계약

- 요청·응답 필드, 오류 상태, 시간 제한이나 내부 인증을 바꾸면 호출하는 백엔드와 배포 설정을 함께 확인한다.
- 기존 CPU 서비스, 실종동물 검색, 갤러리 생성과 사진 처리의 실행 경로를 구분한다.
  한 경로의 변경을 다른 경로의 기본값 변경으로 확대하지 않는다.
- 모델 버전, 벡터 차원, 전처리, 정규화, 점수 방식과 저장된 갤러리의 호환성을 함께 확인한다.
  다른 모델의 벡터를 기존 데이터에 그대로 덮어쓰지 않는다.
- 갤러리 생성의 입력 완전성, 중단·재시작, 상태 기록, 게시 전 검증과 실패 시 기존 검색 유지 계약을 보존한다.
- 이미지·다운로드 처리는 크기, 시간 제한, 실패 응답과 임시 파일 정리를 확인한다.
- GPU 메모리, 동시 추론과 프로세스 수는 해당 실행 프로필의 실제 제한을 따른다.
  가중치 다운로드나 갤러리 전체 재생성을 작은 테스트처럼 실행하지 않는다.

## 검증

- 대상 경로의 Python 버전, 의존성 파일과 기존 테스트 실행 방식을 먼저 확인한다.
  운영 환경과 다른 의존성을 임의 설치해 검증 결과를 맞추지 않는다.
- 가장 좁은 관련 테스트부터 실행한다. 모델 가중치, GPU, DB 또는 외부 서비스가 필요한 검증은
  실제 실행 여부와 환경 제한을 분리해 보고한다.
- API 변경은 정상 응답뿐 아니라 실패·입력 제한·인증 경계를 확인하고,
  저장·갤러리 변경은 재시도·복구·게시 경계를 확인한다.
- 실제 모델을 실행하지 않은 모의 테스트 결과를 검색 품질이나 GPU 운영 검증으로 표현하지 않는다.

## 비밀값·운영 변경·기록

- 운영 `.env`, 인증키, 서명 URL, 가중치 파일, 사진 원본, 갤러리 데이터, 로그와
  `__pycache__` 같은 생성 파일을 새 커밋에 포함하지 않는다. 예시는 비밀값 없는 자리표시자를 쓴다.
- 갤러리 게시·삭제, 검색 alias 전환, DB 변경, 운영 프로세스 재시작과 배포는
  명시적으로 승인된 대상·범위·롤백 방법을 확인한 뒤 실행한다.
- 실제로 발생한 문제는 증상, 원인과 근거, 수정, 검증 결과, 미해결 항목을
  기존 Obsidian 프로젝트 문서에 기록한다. 비밀값을 가리고 저장 후 다시 읽어 확인한다.
